# Base URL of the deployed Worker API. Baked in at build time by Vite.
# Leave unset for local development -- the dev server proxies /api to :8788.
VITE_API_BASE_URL=https://creativescope-api.your-subdomain.workers.dev
